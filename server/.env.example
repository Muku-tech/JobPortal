# server/.env.example - Copy to server/.env and fill real values
# Database (existing)
DB_HOST=localhost
DB_USER=root
DB_PASS=yourpassword
DB_NAME=jobportal

# Email - ADD THESE FOR REAL GMAIL
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=mahatmuku567@gmail.com
SMTP_PASS=your_16char_app_password_here
NODE_ENV=production

# Get app password: https://myaccount.google.com/apppasswords (enable 2FA first)

# JWT (existing)
JWT_SECRET=your_jwt_secret
